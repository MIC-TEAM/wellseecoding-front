import { css } from '@emotion/react'
import { Common } from '../../../styles/common'

export const authLoginTitleWrap = css`
  margin-top: 11em;
`
export const bigTitle = css`
  font-size: ${Common.fontSize.bigTitle};
  font-weight: 500;
  color: ${Common.colors.black};
  margin-top: 27px;
  line-height: 52px;
`
